name: Keep backend awake

on:
  schedule:
    - cron: "*/50 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping WelZone backend
        run: curl -sf "${{ secrets.KOYEB_BACKEND_URL }}/courses" || true